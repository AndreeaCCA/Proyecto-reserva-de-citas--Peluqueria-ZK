<!-- CUERPO DEL EJERCICIO -->
<main>
  <!-- HTML Form (wrapped in a .bootstrap-iso div) -->
  <div class="bootstrap-iso">
    <div class="container-fluid">
      <h3 class="cita">¡Reserva tu cita ya!</h3>
      <div class="row">
        <div class="col-md-4 col-sm-6 col-xs-12">
          {{#each errors}}
          <div class="alert alert-danger" style="color:red" role="alert">
            {{msg}}
          </div>
          {{/each}}
          <form method="post" action="/user">
            <label class="control-label requiredField" for="email">Email: <span class="asteriskField">
                *</span></label>
            <input class="form-control" id="email" name="email" type="email" {{!-- autocomplete="off"  --}}{{!-- required --}} />
        </div>

        <div class="form-group">
          <div>
            <button class="btn btn-dark " name="submit" type="submit" value="Ecriptar mensaje">Citas pendientes</button>
          </div>
        </div>
      </div>
      </form>
      <p><span class="asteriskField">
          * Campo obligatorio
        </span>
      </p>
    </div>
    <div class="col-md-8 col-sm-6 col-xs-12" style="text-align: center;">
      <img src="./images/new.png" alt="haircut" class="img-fluid" style="margin-top:-40px">
    </div>
  </div>
  </div>
</main>